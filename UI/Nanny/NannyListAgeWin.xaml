<Window x:Class="UI.NannyListAgeWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="{DynamicResource listage}" Height="300" Width="300" Icon="/Images/iconanny2.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="249*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="max" Content="{DynamicResource bymax}" HorizontalAlignment="Stretch" Click="max_Click"/>
        <Button x:Name="min" Content="{DynamicResource bymin}" HorizontalAlignment="Stretch" Grid.Column="1" Click="min_Click"/>
        <TextBox x:Name="textBox" HorizontalAlignment="Stretch" Grid.Row="1" TextWrapping="Wrap" Text="" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
