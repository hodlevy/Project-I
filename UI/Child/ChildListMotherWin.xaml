<Window x:Class="UI.ChildListMotherWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="{DynamicResource listbymother}" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="14*"/>
            <RowDefinition Height="255*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBox" HorizontalAlignment="Stretch" Grid.Row="1" TextWrapping="Wrap" Text=""/>
        <ComboBox x:Name="comboBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" FontSize="14" SelectionChanged="comboBox_SelectionChanged">
            <ComboBoxItem Content="{DynamicResource choose}"/>
        </ComboBox>
    </Grid>
</Window>
